# starter-repo2

Minimal starter Python repository.

Quick start:

```powershell
Set-Location C:\Repos\starter-repo2
python -m venv .venv
& .\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip setuptools wheel
python -m pip install -e .
python -m pip install pytest
python -m pytest -q
```
